- name: "install and start tun"
  hosts: "all"
  become: true

  tasks:
  - name: "tuned install"
    apt:
      name: tuned
      state: latest

  - name: "Start tuned"
    systemd:
      name: "tuned"
      enabled: true
      state: started
   